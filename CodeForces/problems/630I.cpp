#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

#define sz(x) int((x).size())
#define bg(x) begin(x)
#define all(x) bg(x), end(x)
#define pb push_back

void solve(){
    int n;
    cin >> n;
    int s = n*2-2;
    int count = n-s;
    int ans = 4*3;
}

int main(){
    int t;
    cin >> t;t=1;
    while(t--){
        solve();
    }
}

