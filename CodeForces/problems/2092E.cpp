#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

#define sz(x) int((x).size())
#define bg(x) begin(x)
#define all(x) bg(x), end(x)
#define pb push_back

void solve(){
    ll n, m, k;
    cin >> n >> m >> k;

    vector<array<int, 3>> grid(k);

    for(int i = 0; i < k; i++){
        cin >> grid[i][0] >> grid[i][1] >> grid[i][2];
    }

    
}

int main(){
    int t;
    cin >> t;
    while(t--){
        solve();
    }
}

