// Source: https://usaco.guide/general/io

#include <bits/stdc++.h>
using namespace std;

typedef pair<int, int> pii
int main() {
    int t;
    cin >> t;
    while(t--){
        vector<int> list(n);
        for(int i= 0; i < n; i++){
            cin >> list[i];
        }
        vector<vector<pii>> adj;
        adj[i] 
    }
}
