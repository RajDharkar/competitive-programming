t = int(input())

def solve():
    x, y, a, b, w = map(int(), input())
    walls = []

    def slope(x, y, x2, y2):
        if x == x2:
            return None    
        return (y - y2) / (x - x2)
    def greater(slope, point)
    for i in range(w):
        one, two, three, four = map(int(), input())
        walls.append([one, two, three, four, slope(one, two, three, four)])
        if 
