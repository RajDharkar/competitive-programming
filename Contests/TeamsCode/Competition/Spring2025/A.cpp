#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

#define sz(x) int((x).size())
#define bg(x) begin(x)
#define all(x) bg(x), end(x)
#define pb push_back

void solve(){
    int n;
    cin >> n;
    string s;
    cin >> s;
    cout << n << endl;
}

int main(){
    int t=1;
    // cin >> t;
    while(t--){
        solve();
    }
}

